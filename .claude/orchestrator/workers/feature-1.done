## feature-1 (pre-work) — DONE

### Completed
- Created local branch sprint/parallel-execution (git checkout -b)
- Wrote docs/learn/project/wave-d-design-choices.md (~235 lines) covering:
  - lora_r=32 rationale and lora_alpha=64 2x scaling rule
  - target_modules choice (in_proj/out_proj/up_proj/down_proj) and Mamba2 rationale
  - apply_loss_mask API contract: inputs, output, what gets masked, IGNORE_INDEX=-100, ValueError guard, immutability guarantee
  - Keep-last-3-plus-best checkpoint rotation policy and sidecar files
  - QualityFilter solver_confidence_threshold gate (default 0.0=disabled, safe-by-default absent=0.0)
  - smoke=50/_SMOKE_LIMIT and cost_cap_usd=$20 limits and --dry-run interaction
- Discarded CRLF drift in feature-*.prompt files via git checkout
- Committed: e62e6cf

### Blocker
- git push -u origin sprint/parallel-execution failed: remote auth token expired.
  Branch exists locally; re-push manually once credentials are refreshed.
