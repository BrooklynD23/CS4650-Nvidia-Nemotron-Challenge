T1.2 complete: wired scripts/hpc/submit_sft.sbatch to scripts/hpc/run_sft.py.

Changes made:
- Replaced Python heredoc stub (lines 122-162) with RESUME_ARG detection via resume_from_latest.py
- Added `python scripts/hpc/run_sft.py --config ... --data-dir ... --output-dir ... $RESUME_ARG 2>&1 | tee -a $RUN_DIR/train.log`
- Added RUN_DIR="${RUN_ROOT}/${RUN_TAG}" to the run-directories section
- Removed 'stub' language from Step 3 comment header
- Kept all surrounding env-var setup, log headers, and exit-code handling (Steps 1, 2, 4)

Commit: feat(hpc): wire submit_sft.sbatch to run_sft.py, remove stub
