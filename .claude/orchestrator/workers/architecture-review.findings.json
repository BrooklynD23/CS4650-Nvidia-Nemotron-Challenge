{
  "summary": "The sprint delivers a coherent training pipeline across six features: a YAML-based config chain resolver, a LoRA launcher, a passthrough data collator, a preflight gate with a --local flag, and a runbook notebook. The core design decisions — separating pre-masking in tokenize_dataset.py from training in run_sft.py, using an extends: inheritance chain for configs, and keeping scripts/hpc/ launchers thin over a src/ library — are sound and well-documented. However, there is a significant naming-convention mismatch between the checkpoint management utilities (checkpoint_policy.py, resume_from_latest.py) that use a step-XXXXX pattern and what HuggingFace/trl SFTTrainer actually writes (checkpoint-XXXXX), rendering both the checkpoint rotation policy and the resume detection effectively inoperative without an adapter layer. A secondary issue is a double invocation of checkpoint_policy logic — once from within run_sft.py and once from submit_sft.sbatch Step 4 — targeting different directories, neither of which contains step-XXXXX dirs.",
  "severity": "moderate",
  "issues": [
    {
      "category": "consistency",
      "severity": "error",
      "file": "scripts/hpc/checkpoint_policy.py",
      "message": "checkpoint_policy.py and resume_from_latest.py both scan for subdirectories matching ^step-(\\d+)$ but trl.SFTTrainer (via HuggingFace Trainer) saves checkpoints as checkpoint-{step}/ (e.g. checkpoint-100). The pattern will never match, so checkpoint rotation silently does nothing and resume detection always returns no checkpoint found.",
      "suggestion": "Either update _STEP_RE = re.compile(r'^checkpoint-(\\d+)$') in both scripts to match HuggingFace Trainer output, or add a custom TrainerCallback that renames checkpoint-N dirs to step-N after each save_steps event. Prefer the former since it eliminates the rename side-effect."
    },
    {
      "category": "coupling",
      "severity": "error",
      "file": "scripts/hpc/submit_sft.sbatch",
      "message": "resume_from_latest.py is called on '$RUN_DIR/checkpoints' (line 124) but run_sft.py is told --output-dir '$RUN_DIR' (line 131), so the Trainer writes checkpoints to $RUN_DIR/checkpoint-* while the resume scanner looks in $RUN_DIR/checkpoints/. If $CHECKPOINT_ROOT differs from $RUN_ROOT, $CHECKPOINT_DIR diverges further. The resume path is structurally guaranteed to find nothing on first invocation.",
      "suggestion": "Pass a consistent checkpoint sub-directory to both run_sft.py (--output-dir \"$CHECKPOINT_DIR\") and resume_from_latest.py, or make resume_from_latest.py accept the same $RUN_DIR and scan one level down."
    },
    {
      "category": "pattern",
      "severity": "warning",
      "file": "scripts/hpc/run_sft.py",
      "message": "_run_checkpoint_policy() is called inside run_sft.py at line 366 with --execute (always active), and submit_sft.sbatch Step 4 independently calls checkpoint_policy.py on $CHECKPOINT_DIR. These two calls target different directories ($RUN_DIR vs $CHECKPOINT_DIR) and both will silently find zero step-XXXXX dirs. The duplication obscures which layer owns checkpoint lifecycle management.",
      "suggestion": "Remove the _run_checkpoint_policy() call from run_sft.py main() and let the orchestrating sbatch script own checkpoint lifecycle as a separate explicit step. A launcher should not call post-processing that the orchestrator also calls."
    },
    {
      "category": "abstraction",
      "severity": "warning",
      "file": "scripts/hpc/run_sft.py",
      "message": "PassthroughCollator pads input_ids with token ID 0 hardcoded (line 259) instead of the tokenizer's pad_token_id. The tokenizer is not injected into the collator. Padding with 0 is harmless when attention_mask correctly masks those positions, but it violates the principle of using the model's canonical pad token and can cause confusion if 0 happens to be a real vocab token.",
      "suggestion": "Accept tokenizer (or just pad_token_id: int) as a constructor argument to PassthroughCollator and use it for input_ids padding."
    },
    {
      "category": "consistency",
      "severity": "warning",
      "file": "configs/smoke_sft_local.yaml",
      "message": "smoke_sft_local.yaml sets target_modules: [q_proj, k_proj, v_proj, o_proj] (attention heads, correct for Qwen2.5-0.5B-Instruct) while lora_baseline.yaml uses [in_proj, out_proj, up_proj, down_proj] (Mamba2 SSM modules for the actual competition model). The divergence is architecturally correct but undocumented inline; a reader may assume local smoke validates the same LoRA wiring as production.",
      "suggestion": "Add a comment to smoke_sft_local.yaml: '# target_modules override for Qwen2.5 attention architecture; production uses in_proj/out_proj/up_proj/down_proj for Mamba2.'"
    },
    {
      "category": "pattern",
      "severity": "warning",
      "file": "scripts/hpc/run_sft.py",
      "message": "Both run_sft.py and tokenize_dataset.py mutate sys.path at module import time (sys.path.insert(0, str(_REPO_ROOT))). This is a side-effect that changes the import environment for all downstream code in the process and can cause hard-to-diagnose import shadowing if the repo root contains a module that conflicts with an installed package.",
      "suggestion": "Use PYTHONPATH=$REPO_ROOT in the shell environment (set in submit_sft.sbatch or the venv activation) rather than mutating sys.path at import time."
    },
    {
      "category": "abstraction",
      "severity": "warning",
      "file": "scripts/hpc/run_sft.py",
      "message": "load_config() has no schema validation after resolving the extends: chain. Required keys (base_model, lora_r, target_modules) are accessed with cfg.get() with silent fallbacks, so a misconfigured or broken extends chain produces a confusing runtime error deep in model loading rather than a fast-fail at config parse time.",
      "suggestion": "Add a lightweight validation step after load_config() that asserts required keys are present and non-empty before the expensive model download begins."
    },
    {
      "category": "scalability",
      "severity": "info",
      "file": "scripts/hpc/run_sft.py",
      "message": "ShardDataset loads all .pt shards fully into CPU memory in __init__ (self._examples is a flat Python list). For the current smoke scale (100-1000 examples) this is fine, but at full dataset scale the memory footprint could be significant before training even begins.",
      "suggestion": "For future scale, consider lazy shard loading: hold a list of (shard_path, offset) pointers and load one shard at a time in __getitem__. This is a known-future-requirement, not a current blocker."
    },
    {
      "category": "pattern",
      "severity": "info",
      "file": "scripts/hpc/run_sft.py",
      "message": "torch.load(..., weights_only=False) at line 214 silences PyTorch's pickle-safety warning. In this closed pipeline (shards produced by the same repo) this is safe, but it disables the security guard that catches malicious .pt files if the data directory were ever populated from an external source.",
      "suggestion": "Add an inline comment documenting the assumption: '# weights_only=False: shards are produced by this repo; safe to unpickle.' If the data directory is ever shared with external write access, switch to weights_only=True with appropriate safe_globals registration."
    }
  ],
  "recommendations": [
    "Fix the step-XXXXX vs checkpoint-XXXXX naming mismatch as the highest priority — checkpoint rotation and resume detection are both silently broken, meaning each job starts from scratch and disk accumulates unbounded checkpoints.",
    "Consolidate checkpoint lifecycle ownership into submit_sft.sbatch exclusively; remove the _run_checkpoint_policy() call from run_sft.py to eliminate dual-invocation on mismatched directories.",
    "Wire a consistent output directory through the whole sbatch pipeline: pass --output-dir \"$CHECKPOINT_DIR\" to run_sft.py and point resume_from_latest.py at the same directory so all three steps (train, resume, rotate) share one path.",
    "Inject pad_token_id into PassthroughCollator at construction time rather than hardcoding 0, to make the collator's contract explicit and safe for any tokenizer.",
    "Replace sys.path mutation in both launcher scripts with PYTHONPATH= in the sbatch environment; this is already the HPC convention and removes the import-shadowing risk.",
    "Add a config schema validation step (required keys + type assertions) immediately after load_config() in run_sft.py so misconfiguration fails fast before the model download begins.",
    "Add an inline comment to smoke_sft_local.yaml explaining the target_modules divergence from production to prevent a future reader from treating smoke success as proof that the Mamba2 LoRA wiring is correct."
  ]
}
