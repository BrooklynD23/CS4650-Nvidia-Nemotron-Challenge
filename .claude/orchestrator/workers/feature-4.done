T1.3 complete.

Task A — configs/smoke_sft_local.yaml:
  Created with header "LOCAL SMOKE ONLY — NOT FOR SUBMISSION", extends configs/smoke_sft.yaml,
  overrides: base_model=Qwen/Qwen2.5-0.5B-Instruct, lora_r=8, lora_alpha=16,
  target_modules=[q_proj,k_proj,v_proj,o_proj], dataset_max_rows=100, max_steps=20, save_steps=10.

Task B — scripts/hpc/preflight.sh --local flag:
  Added LOCAL=0 variable, --local to argument parser and usage string.
  Section 1: skips frozen BASE_MODEL_ID equality check when LOCAL=1 (logs current value instead).
  Section 3: skips model weights path check entirely when LOCAL=1.
  Sections 2, 4, 5, 6 (CUDA, dataset SHA256, disk, packages) run unconditionally.
  Default behavior (no --local) is unchanged.

Commit: 7d6ebb3 feat(hpc): add smoke_sft_local.yaml and preflight --local flag
