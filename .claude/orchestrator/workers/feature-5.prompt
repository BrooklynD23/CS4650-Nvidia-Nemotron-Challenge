You are a worker agent focused on implementing a single feature.

## Your Task
T2.4: Fill notebooks/09_sft_runbook_and_masking.ipynb beyond its current stub (~200 lines). Add cells: (1) masking walkthrough loading a real SFTExample, showing tokenizer output, calling apply_loss_mask, rendering which positions are kept vs masked; (2) pipeline diagram markdown: preflight → tokenize_dataset → run_sft → checkpoint_policy → regression_gate → package_adapter; (3) reference CLI invocation cells (do NOT execute training, just show commands for run_sft.py with lora_baseline.yaml and the sbatch submission); (4) resume semantics showing how resume_from_latest.py feeds --resume-from-checkpoint; (5) promotion criteria: golden gate must pass before package_adapter.py runs. Update docs/execution/NOTEBOOKS.md: notebook 09 status scaffolded → active.

## Orchestration Context
Sprint: Parallel Execution — implement the plan at docs/execution/plans/sprint-parallel-execution.md. Two parallel tracks: Track 1 (training pipeline) and Track 2 (data/eval/notebooks). Current branch: main (sprint branch to be created). Key missing deliverables: scripts/hpc/run_sft.py (the SFT training launcher replacing the stub in submit_sft.sbatch), configs/smoke_sft_local.yaml, preflight.sh --local flag, and notebook 09 fill. Pre-work also needed: wave-d retrospective doc and branch creation.

## Project Context Files
### From CLAUDE.md:
# CLAUDE.md (Router)

Route your task to the right docs. Keep context minimal — open only what your task needs.

| Task | Go to |
|------|-------|
| Competition rules, dataset, scoring | `docs/architecture/COMPETITION.md` |
| System / model design | `docs/architecture/ARCHITECTURE.md` |
| Execution plan (source of truth) | `docs/planning/plan_v0.2.md` |
| Sprint / issue tracking | `docs/execution/SPRINTS.md` |
| Notebook registry & status | `docs/execution/NOTEBOOKS.md` |
| Current repo state | `docs/learn/project/implemented-today.md` |
| Analysis & adversarial review | `docs/analysis/` |
| All docs index | `docs/README.md` |

## Working rules

- Reusable code → `src/`; keep notebooks thin.
- Large artifacts (`data/`, `adapters/`, `experiments/`) stay out of git.
- Every notebook must have a row in `docs/execution/NOTEBOOKS.md`.



## Instructions
1. Focus ONLY on implementing this specific feature
2. Make small, incremental changes
3. Test your changes as you go
4. When you are DONE:
   - Commit your changes following conventional commit format:
     * Use: feat(scope), fix(scope), docs(scope), refactor(scope), test(scope)
     * Example: "feat(auth): add user authentication system"
     * Add footer: "🤖 Committed by claude-swarm worker feature-5"
   - Create a file at: .claude/orchestrator/workers/feature-5.done
     with a brief summary of what you implemented

## Important
- Do not work on other features
- If you encounter a blocker, document it in the .done file and stop
- Keep changes minimal and focused
- Always commit using conventional commit format with the worker attribution footer
- The .claude/ directory is already gitignored - don't worry about it

## Commit Format Example
```bash
# Add only the specific files you changed for this feature
git add src/path/to/file1.ts src/path/to/file2.ts

# Commit with subject and attribution footer
git commit -m "feat(feature-name): implement the feature" -m "🤖 Committed by claude-swarm worker feature-5"
```


## Additional Context
T2.4: Fill notebooks/09_sft_runbook_and_masking.ipynb beyond its current stub.\n\nFirst read the current notebook to understand its structure. Also read:\n- scripts/hpc/run_sft.py (for accurate CLI flags)\n- scripts/hpc/submit_sft.sbatch (for accurate sbatch invocation)\n- scripts/hpc/resume_from_latest.py (for resume semantics)\n- src/training/sft_trainer.py (for apply_loss_mask signature)\n- src/contracts.py (for SFTExample schema)\n- configs/lora_baseline.yaml (for the config used in examples)\n\nAdd notebook cells covering these sections (add to existing stub, do not remove existing cells):\n\n1. **Masking walkthrough cell** (code): Import SFTExample and apply_loss_mask from src. Create a minimal SFTExample dict. Show tokenizer output (use a small tokenizer like gpt2 or just demonstrate with dummy tensors if no tokenizer is available locally). Call apply_loss_mask. Print which token positions are labels=-100 (masked) vs actual token IDs (kept). Use markdown to explain what is happening.\n\n2. **Pipeline diagram cell** (markdown): ASCII or text diagram showing:\n   `preflight.sh → tokenize_dataset.py → run_sft.py → checkpoint_policy.py → regression_gate.py → package_adapter.py`\n   with brief description of each stage's input/output.\n\n3. **Reference CLI invocation cells** (code cells with # comments only, do NOT execute training): Show the exact CLI commands:\n   - `python scripts/hpc/run_sft.py --config configs/lora_baseline.yaml --data-dir /path/to/tokenized --output-dir /path/to/run`\n   - `sbatch scripts/hpc/submit_sft.sbatch configs/lora_baseline.yaml`\n   Use `print()` statements or just comments showing the commands.\n\n4. **Resume semantics cell** (code + markdown): Show how `resume_from_latest.py` returns the latest checkpoint path, and how that feeds `--resume-from-checkpoint` in run_sft.py.\n\n5. **Promotion criteria cell** (markdown): Explain that `regression_gate.py` must pass (exit 0) against `data/golden_20.jsonl` before `package_adapter.py` is run to produce the final submission.zip.\n\nThen update docs/execution/NOTEBOOKS.md: change notebook 09 status from `scaffolded` to `active`.\n\nCommit: git add notebooks/09_sft_runbook_and_masking.ipynb docs/execution/NOTEBOOKS.md && git commit -m 'feat(notebooks): fill notebook 09 SFT runbook with masking walkthrough and pipeline reference'

Begin implementing the feature now.