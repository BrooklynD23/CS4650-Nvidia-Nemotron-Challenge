You are a worker agent focused on implementing a single feature.

## Your Task
T1-C: Complete notebooks/08_synthetic_data_recipe.ipynb beyond its current 14-cell scaffold. The notebook exists but lacks executable generation content. Read the current notebook first. Then update: Cell [7] bind llm_teacher_fn with os.environ.get('LLM_API_KEY') guard — if absent use a stub lambda that returns '\boxed{STUB}'. Cell [9] add actual generation loop calling generate_from_retry_candidates(candidates, config, smoke=True) from src.data.synthetic. Cell [10] fingerprint verification using Path(output_path + '.sha256').read_text(). Cell [11] summary table showing rows generated/rejected/cost/fingerprint. Strip all cell outputs before saving (existing repo convention — use nbstripout or clear outputs manually in the JSON).

## Orchestration Context
Close the remaining implementation gaps identified in issues #10/#24 (synthetic data) and #11/#25 (SFT training pipeline) so the project can start fine-tuning. The approved plan is at /home/danny/.claude/plans/elegant-floating-russell.md. Key constraint: base model is metric/nemotron-3-nano-30b-a3b-bf16/transformers/default, lora_r <= 32, answers use \boxed{} format, SFTExample schema is frozen in src/contracts.py:97.

## Project Context Files
### From CLAUDE.md:
# CLAUDE.md (Router)

Route your task to the right docs. Keep context minimal — open only what your task needs.

| Task | Go to |
|------|-------|
| Competition rules, dataset, scoring | `docs/architecture/COMPETITION.md` |
| System / model design | `docs/architecture/ARCHITECTURE.md` |
| Execution plan (source of truth) | `docs/planning/plan_v0.2.md` |
| Sprint / issue tracking | `docs/execution/SPRINTS.md` |
| Notebook registry & status | `docs/execution/NOTEBOOKS.md` |
| Current repo state | `docs/learn/project/implemented-today.md` |
| Analysis & adversarial review | `docs/analysis/` |
| All docs index | `docs/README.md` |

## Working rules

- Reusable code → `src/`; keep notebooks thin.
- Large artifacts (`data/`, `adapters/`, `experiments/`) stay out of git.
- Every notebook must have a row in `docs/execution/NOTEBOOKS.md`.



## Instructions
1. Focus ONLY on implementing this specific feature
2. Make small, incremental changes
3. Test your changes as you go
4. When you are DONE:
   - Commit your changes following conventional commit format:
     * Use: feat(scope), fix(scope), docs(scope), refactor(scope), test(scope)
     * Example: "feat(auth): add user authentication system"
     * Add footer: "🤖 Committed by claude-swarm worker feature-5"
   - Run the following verification commands and ensure they pass:
     * `pytest tests/data/test_synthetic.py tests/data/test_gan_refine.py -v --tb=short`
     * `pytest tests/ -v --tb=short --ignore=tests/data/test_synthetic.py --ignore=tests/data/test_gan_refine.py -q`
     * If any verification command fails, fix the issue before proceeding
   - Create a file at: .claude/orchestrator/workers/feature-5.done
     with a brief summary of what you implemented

## Important
- Do not work on other features
- If you encounter a blocker, document it in the .done file and stop
- Keep changes minimal and focused
- Always commit using conventional commit format with the worker attribution footer
- The .claude/ directory is already gitignored - don't worry about it

## Commit Format Example
```bash
# Add only the specific files you changed for this feature
git add src/path/to/file1.ts src/path/to/file2.ts

# Commit with subject and attribution footer
git commit -m "feat(feature-name): implement the feature" -m "🤖 Committed by claude-swarm worker feature-5"
```


## Additional Context
Read notebooks/08_synthetic_data_recipe.ipynb first (it is a JSON file) to understand the current 14-cell scaffold before editing. The generate_from_retry_candidates function now accepts prompt_templates as a parameter (added in the previous batch). Import from src.data.synthetic. Use os.environ.get('LLM_API_KEY') to decide between real and stub teacher. Strip all cell outputs from the notebook JSON before saving — set 'outputs': [] and 'execution_count': null on every code cell. Do NOT run nbstripout as a command; manually set outputs to empty in the JSON.

Begin implementing the feature now.