# src/

Reusable project code lives here. Notebooks should import from `src/` instead of duplicating logic.

Planned modules (see `docs/architecture/ARCHITECTURE.md`):

- dataset ingestion + normalization
- prompting / formatting helpers
- teacher / solver utilities
- training + evaluation utilities
- submission packaging
